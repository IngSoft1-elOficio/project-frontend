// Estado inicial
export const initialState = {
  // vacío por ahora
};

// Tipos de acción
export const actionTypes = {
  // agregar luego
};

// Reducer
export const appReducer = (state, action) => {
  switch (action.type) {
    // Agregar casos
    default:
      return state;
  }
};



